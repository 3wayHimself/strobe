#include <bioslib.str>
#include <math.str>
namespace Name
{
    function Main()
    {
		$test = new(true);
		label($hello);

		proc_Add(*10,$hello);
		proc_Add(*0,$hello);
		proc_Add(*0,$hello);
		proc_Add(*0,$hello);
		proc_Add(*0,$hello);
		proc_Add(*0,$hello);
		proc_Add(*0,$hello);
		proc_Add(*0,$hello);
		proc_Add(*0,$hello);

		Math.Write(*0);

		goto($hello,$test);
    	System.Exit(0);
    }
}